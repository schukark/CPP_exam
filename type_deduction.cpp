#include <iostream>

#include <vector>

typename std::iterator<std::vector <int>> x = *itre

auto x = obj -> приводит к типу автоматически

const auto

const auto&

auto&&

int&& a = 1;

auto x = a; -> плохо

decltype(a) b -> тип соответсвует изначальному

decltype(a++) т.к. это функц. сущность

decltype(auto) -> смотрит что возвращается и делать decltype()

